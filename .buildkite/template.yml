name: "Code Review Pipeline Example"
description: "An example pipeline that uses dynamic pipelines and Claude Code to automatically review GitHub pull requests."
emoji: ":mag:"
languages:
  - "TypeScript"
steps:
  - label: ":pipeline:"
    command: "buildkite-agent pipeline upload"
